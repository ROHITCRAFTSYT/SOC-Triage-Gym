# Direct dependencies — minimum versions set to address known CVEs
fastapi>=0.115.6
uvicorn>=0.34.0
pydantic>=2.10.6
pydantic-core>=2.27.2
pyyaml>=6.0.2
# python-multipart >=0.0.22 fixes Arbitrary File Write (CVE-2025-*, High)
python-multipart>=0.0.22
httpx>=0.28.1
anyio>=4.8.0
# starlette >=0.52.1 fixes O(n^2) DoS via Range header (High) and multipart DoS (Moderate)
starlette>=0.52.1
openai>=1.0.0
openenv-core>=0.2.0
# cryptography >=44.0.0 fixes buffer overflow on non-contiguous buffers (Moderate)
cryptography>=44.0.0
# fastmcp >=3.2.0 fixes SSRF & Path Traversal (Critical), OAuth Confused Deputy (High),
# and Command Injection via Gemini CLI (Moderate)
fastmcp>=3.2.0
# mcp >=1.28.1 (pulled in by fastmcp) fixes cross-client task access in the
# experimental task handlers (High), missing authenticated-principal checks on the
# HTTP transports (High), and missing Host/Origin validation on the WebSocket
# server transport (High)
mcp>=1.28.1
# pillow >=12.3.0 (pulled in by openenv-core -> gradio) fixes a batch of heap
# out-of-bounds writes (ImagingExpand/RankFilter, ImageCmsTransform.apply,
# Image.paste/crop), an OOB read on the McIdas mmap path, several decompression-bomb
# bypasses (PDF, BDF/PCF/FontFile), JPEG2000 and EPS DoS, TGA heap disclosure, and
# WindowsViewer command injection
pillow>=12.3.0
